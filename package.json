{
  "name": "genesistherealm",
  "version": "1.0.0",
  "description": "Website for the Genesis community",
  "main": "index.js",
  "scripts": {
    "dev": "npx astro dev",
    "build": "npx astro build",
    "deploy": "npm run build && rm -rf dist/* && cp -r build/* dist/ && cd dist && git add . && git commit && git push && cd ..",
    "deploydev": "npm run build && rm -rf devdist/* && cp -r build/* devdist/ && cd devdist && git add . && git commit && git push && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lilacstella/GenesisTheRealm.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/lilacstella/GenesisTheRealm/issues"
  },
  "homepage": "https://github.com/lilacstella/GenesisTheRealm#readme",
  "dependencies": {
    "astro": "^5.7.12"
  }
}
